# Open Questions

- Where do reminders go once in-app is not enough? Email digest is the cheap
  option; WhatsApp is the one actually wanted but is gated behind a privacy
  analysis in prismo canon.
- Does the TFSA total need to span multiple providers? The caps are per person,
  not per account. Currently handled by a manual "prior years" figure, which
  silently assumes the user aggregates correctly.
- What is the right model for the "treat myself" bucket — a savings goal with a
  different label, or a distinct concept with its own rules?
- Should the tax rebate claim be tracked as a workflow (documents gathered,
  submitted, assessed, paid) or just a reminder?
